codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: 0
        threshold: 0
    patch:
      default:
        target: 0
        threshold: 0

ignore:
  - "clustintime/tests/"
  - "clustintime/due.py"
  - "clustintime/_version.py"
  - "clustintime/info.py"
